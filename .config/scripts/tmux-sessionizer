#!/bin/bash

if [[ $# -eq 1 ]]; then
    selected=$1
else
    selected=$(find /media/storage/development/python -mindepth 1 -maxdepth 1 -type d | fzf --reverse --border)
fi

if [[ -z $selected ]]; then
    exit 0
fi

selected_name=$(basename "$selected" | tr . _)
tmux_running=$(pgrep tmux)


tmux new-session -A -s $selected_name -c $selected

